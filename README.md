## Run on Bluemix Container

* `git clone https://github.com/alohr51/Microservices-Catalog-Container.git`
* `cd Microservices-Catalog-Container`
* `cf login`
* `cf ic login`
* Build the docker image
* Launch the container from the image created above

